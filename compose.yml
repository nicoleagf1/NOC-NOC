services:
  noc-noc-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: noc-noc-app
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    restart: unless-stopped
    environment:
      - NODE_ENV=production
